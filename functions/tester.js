
const arrayUtil = require('./array_util');

const result = arrayUtil.returnLastN([1, 2, 3, 4], 2);

console.log(result);

// This is just an example to show that the arrayUtil is an object.
console.log(arrayUtil.name);